# Legacy Next.js Prototype

Este diretório preserva o protótipo original do `labia-chat` em Next.js/React.

O novo desenvolvimento ativo do projeto será feito no backend FastAPI, em `backend/`.

Este código deve ser tratado como referência histórica/visual, não como base ativa do MVP atual.
